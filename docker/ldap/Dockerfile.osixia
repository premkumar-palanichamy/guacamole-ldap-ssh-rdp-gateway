FROM osixia/openldap:1.5.0

# Reference-only Dockerfile (legacy approach).
# Not used by docker-compose.yml.
# Kept to show the previous LDAP image strategy.
COPY ldap/bootstrap /container/service/slapd/assets/config/bootstrap/ldif/custom
